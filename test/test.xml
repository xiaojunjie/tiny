using namespace WebX;
int main(int argc, char *argv[])
{
    WebApp<xxx, yyy, zzz,.....> app(argc, argv);

    app.route("xxxx/index", [](Contex ctx){
        YourClass data;
        Template template<YourClass>("path/to/index.template", data);
        .....
        Respone res(template.render(), 200);
        return res;
    });

    return app.run();
}
